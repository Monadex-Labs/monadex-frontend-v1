import PageHeader from '@/components/Swap/SwapHeader'
const Pool = () => {
    return (
        <div>
            <PageHeader isTablet={false} pageName='Pools'/>
        </div>
    )
}

export default Pool